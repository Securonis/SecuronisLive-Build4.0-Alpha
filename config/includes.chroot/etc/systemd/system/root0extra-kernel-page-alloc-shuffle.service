[Unit]
Description=Root0Extra - Memory page allocator randomization
DefaultDependencies=no
Before=network.target
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/sysctl -w vm.page_alloc.shuffle=1
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
